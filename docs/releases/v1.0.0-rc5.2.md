# RuleScript v1.0.0-rc5.2

## Patch Scope

- Fixes debug sessions when hosts configure async runtime event handlers.

## Fixed

- `RuleScriptDebugSession` now captures both synchronous and asynchronous runtime event handlers while a debug run is active.
- Breakpoints and step pauses are now observed when the host has configured `RuntimeEventHandlerAsync`.
- Original runtime event handlers are restored after a debug run completes or fails.

## Validation

- Added regression coverage for `RuleScriptDebugSession` breakpoints when `RuntimeEventHandlerAsync` is configured.
- Full test suite passed.
- Passed: 304
- Failed: 0
- Skipped: 0
