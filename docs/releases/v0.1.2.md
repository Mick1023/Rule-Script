# RuleScript v0.1.2

## Added

- NuGet publish workflow
- GitHub Actions package pipeline
- NuGet package metadata

## Improvements

- Packaging reliability
- Release automation

## Validation

- Build passed
- Tests passed
- NuGet package generated

## Notes

This release prepares `RuleScript.Core` for publishing to NuGet.org through the GitHub Actions package workflow. Publishing uses the `NUGET_API_KEY` GitHub repository secret; no API key is stored in the repository.
