# RuleScript v0.2.0

## Added

- Added `while` / `endwhile`
- Added `break`
- Added `continue`
- Added max loop iteration safety

## Notes

- No `delay` or `waitfor` syntax was added.
- Use Host Functions for timing or external waits, such as `Delay(1000)` or `WaitFor("Ready", 5000)`.

## Validation

- Build passed
- Tests passed
- NuGet package generated
