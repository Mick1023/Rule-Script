# RuleScript v1.0.0-rc5.3

## Patch Scope

- Hardens debug session event forwarding and verifies breakpoint, continue, and step-over behavior.

## Fixed

- Non-pause events such as `Print` are forwarded to the original runtime event handlers during debug sessions.
- `RuleScriptDebugSession` continues to own breakpoint and step resume control while forwarding observable events to host handlers.

## Validation

- Added regression coverage for `RuleScriptDebugSession` forwarding `Print` events to `RuntimeEventHandlerAsync`.
- Added regression coverage that `RunAsync` pauses on a line 5 breakpoint and continues through the remaining script.
- Added regression coverage that repeated `StepOver` advances one executable statement at a time and waits for the next resume command.
- Added coverage that `RuntimeEventHandlerAsync` receives `BreakpointHit`, `StepPaused`, `Error`, and `Print` events.
- Full test suite passed.
- Passed: 308
- Failed: 0
- Skipped: 0
