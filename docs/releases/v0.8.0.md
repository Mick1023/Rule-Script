# RuleScript v0.8.0

## Added

- Added string standard library functions.
- Added array standard library functions.
- Added math standard library functions.
- Added conversion helper functions.
- Added `JsonExists(value, path)`.
- Added `Coalesce(a, b)`.

## String Functions

- `StartsWith(text, value)`
- `EndsWith(text, value)`
- `Contains(text, value)`
- `Split(text, separator)`
- `Join(separator, values)`

## Array Functions

- `ArrayAdd(array, value)`
- `ArrayRemove(array, value)`
- `ArrayContains(array, value)`
- `ArrayClear(array)`

## Math Functions

- `Abs(number)`
- `Min(a, b)`
- `Max(a, b)`
- `Clamp(value, min, max)`
- `Round(value)`
- `Floor(value)`
- `Ceiling(value)`

## Conversion Functions

- `ParseBool(value)`
- `IsNull(value)`
- `TypeOf(value)`

## JSON Helpers

- `JsonExists(value, path)`

## Runtime Helpers

- `Coalesce(a, b)`

## Documentation

- Cleaned up README supported features.
- Renamed `Not Supported Yet` to `Roadmap / Planned Features`.
- Updated built-in function documentation.

## Validation

- Build passed
- Tests passed
- NuGet package generated
